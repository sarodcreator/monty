#include "monty.h"

/**
 * _nop - func to do nothing
 * @front: pointer to pointer of first node
 * @count: line counter
 *
 * Return: none
 */

void _nop(stack-t **front, unsigned int count)
{
	count = count;
	front = front;
}
