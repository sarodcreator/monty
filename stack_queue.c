/**
 * _stack - free stack
 * @front: first node (not used)
 * @count: line counter (not used)
 *
 * Return: none
*/

void _stack(stack_t **front, unsigned int count)
{
	head = head;
	count = count;

	global.lifo = 0;
}

/**
 * _queue - print first node of queue
 * @front: first node of queue (not used)
 * @count: line counter (not used)
 *
 * Return: none
 */

void _queue(stack_t **head, unsigned int count)
{
	head = head;
	count = count;

	global.lifo = 1;
}
